---
# --- Metadatos Básicos ---
title: "Análisis Transaccional de venta minorista"
author:
   - Radames Montiel
   - Rafael Ferrer
date: "Febrero 2026"

# --- Metadatos Específicos de Plantilla ---
degree: "Computación I"
director: 
  - Prof. Jesus Ochoa 
  - Prof. Oliver Triveño
lugar: "Caracas"
facultad: 
  - Facultad de Ciencias Económicas y Sociales
  - Escuela de Estadistica y Ciencias Actuariales
# Ruta al escudo (relativa al archivo .Rmd)
escudo: "Latex/Classes/Escudos/faces"

# --- Bibliografía ---
bibliography: bibliografia.bib

# --- Configuración de Salida ---
output: 
  pdf_document:
    template: template.tex  # Llamamos al archivo de plantilla
    keep_tex: true                 # Útil para ver errores si ocurren
    toc: true                      # Generar índice
    number_sections: true
    citation_package: natbib       # Importante: forzar natbib porque tu plantilla lo usa

# --- Resumen (Abstract) ---
abstract: |
    En el presente informe se realiza un análisis exploratorio y descriptivo de una base de datos transaccional del sector retail. Se examina el impacto de las características demográficas de los clientes (edad y género) sobre el comportamiento de compra, la preferencia de categorías de productos y el volumen total de facturación. Mediante la aplicación de técnicas de estadística descriptiva, segmentación de datos y visualización interactiva, el estudio identifica los patrones de consumo clave y perfila al cliente de mayor rentabilidad para respaldar la toma de decisiones comerciales.
---

```{r setup, include=FALSE}
# Configuración global de chunks
knitr::opts_chunk$set(
  echo = FALSE,        # Mostrar código
  warning = FALSE,    # Ocultar advertencias
  message = FALSE,    # Ocultar mensajes de carga
  fig.align = "center",
  out.width = "90%",
  fig.pos = "H",
  out.extra = ""
)

# Cargar librerías necesarias
library(tidyverse)
library(knitr)
library(kableExtra)
library(scales)
```

\chapter*{Introducción}

Rentabilidad de la venta minorista mediante el análisis de la segmentación demográfica y el comportamiento de las transacciones

En el sector minorista, conocer el volumen general de ventas no es suficiente; el verdadero valor estratégico radica en entender qué perfiles de clientes generan esas ventas.

En lo que a ventas se refiere, poder conocer no solo la cantidad de ventas sino también qué clientes las produce, genera el mayor valor para una empresa, ya que esta información puede llevar a que se tomen decisiones en función de permitir la entrada de más clientes en los sectores con mejor desempeño. Estas decisiones no solo están basadas en la intuición, sino también basadas en datos, lo que podrá ser de gran ayuda en la inversión en marketing y optimización del Retorno de Inversión (ROI).

Por todo lo mencionado, el siguiente informe busca responder la pregunta: ¿Cuáles son los grupos demográficos que generan mayor rentabilidad y más transacciones en la tienda minorista?.

\section*{Estructura del Informe}

Este documento técnico se encuentra estructurado en cinco capítulos fundamentales:

\begin{itemize}
    \item En el \textbf{Capítulo I}, se plantea la problemática de la imprevisibilidad financiera en el desarrollo de software de entretenimiento, formulando las preguntas de investigación y delimitando el alcance temporal y geográfico del estudio.
    
    \item El \textbf{Capítulo II} establece el Marco Teórico, definiendo los conceptos estadísticos clave como esperanza matemática, series de tiempo y correlación, necesarios para la interpretación de los datos.
    
    \item El \textbf{Capítulo III} describe el Marco Metodológico, detallando el proceso de limpieza de datos (ETL), las pruebas de hipótesis y los algoritmos utilizados para validar la calidad de la información.
    
    \item El \textbf{Capítulo IV} presenta el Análisis de Resultados, donde se exhiben las tablas y gráficos generados dinámicamente, discutiendo los hallazgos sobre la concentración de mercado (Ley de Pareto) y la dinámica temporal.
    
    \item Finalmente, en el \textbf{Capítulo V}, se exponen las Conclusiones y Recomendaciones estratégicas derivadas del análisis cuantitativo, orientadas a la toma de decisiones basada en datos (\textit{Data-Driven Decision Making}).
\end{itemize}

Este reporte no solo pretende ofrecer una radiografía del pasado de la industria, sino también establecer una metodología reproducible para el análisis de riesgo en portafolios de productos digitales.

\chapter{Planteamiento del Problema} 

En el entorno altamente competitivo del sector minorista (retail), el éxito financiero de una empresa ya no depende únicamente de la cantidad de productos que vende, sino de su capacidad para comprender a profundidad quiénes son los clientes que generan dichas ventas. Actualmente, las empresas recopilan un volumen considerable de datos transaccionales diarios que incluyen perfiles demográficos (edad y género), hábitos de compra (categorías y cantidades) y m´etricas de impacto financiero (precio unitario y monto total).
Sin embargo, el problema radica en que, sin un análisis adecuado, esta información se mantiene como datos crudos. La empresa se enfrenta a una incertidumbre estratégica al no tener claramente identificados los patrones de consumo de sus clientes. Al no conocer cómo las variables demográficas impactan la rentabilidad, se corre el riesgo de invertir en campañas de marketing ineficientes, mantener un inventario desalineado con las preferencias reales de los compradores y perder oportunidades valiosas de ventas cruzadas y fidelización de los segmentos de mayor valor.
Ante esta situación, surgen interrogantes fundamentales: ¿Son los jóvenes o los adultos quienes sostienen financieramente el negocio? ¿Existe una brecha de género en el ticket promedio de compra? La ausencia de respuestas basadas en datos a estas interrogantes genera ineficiencias operativas y financieras.

Argumento final. Debido a esto, se plantea las siguientes preguntas de investigación:

**1.** ¿Varía el ticket promedio significativamente entre hombres y mujeres?

**2.** ¿Cómo influye la edad de manera directa en la elección de la categoría del producto?

**3.** Mientras mayor edad tienen los clientes, ¿hay tendencia a comprar artículos premium y a un mayor gasto total?

\section{Justificación} 

La justificación de este trabajo de investigación reside en la necesidad de aplicar técnicas de análisis de datos e Inteligencia de Negocios para transformar registros transaccionales en conocimiento estratégico.
Desde un punto de vista práctico, los resultados de este estudio permitirán a la gerencia pasar de la intuición a la toma de decisiones basadas en datos. Al identificar con exactitud el perfil del Cliente Ideal) y comprender la rentabilidad segmentada, la empresa podrá optimizar su Retorno de Inversión (ROI), focalizar sus presupuestos de marketing en los segmentos correctos y adaptar el surtido de productos (económicos vs. premium).


\section{Objetivos} 

\subsection{Objetivo General} 

Analizar los patrones demográficos y el comportamiento de compra en los registros transaccionales del sector retail, con el fin de caracterizar su impacto en la facturación y proporcionar insights que permitan optimizar las estrategias comerciales de la empresa.


\subsection{Objetivos Específicos} 

\begin{enumerate}
  \item Determinar si existe una variación significativa en el ticket promedio de compra (Total Amount) entre hombres y mujeres.
  \item Evaluar de qué manera influye la edad del cliente (agrupada en menores y mayores de 40 años) en la elección de la categoría del producto (Ropa, Belleza, Electrónica).
  \item Contrastar la hipótesis de tendencia de compra Premium, verificando si a mayor edad los clientes tienden a comprar artículos de lujo (> \$300) y generar un mayor gasto total.
  \item Identificar qué rango de edad representa el mayor volumen de ingresos acumulados (motor financiero) para el negocio.
  \item Comprobar si el segmento demográfico que realiza la mayor cantidad de transacciones (volumen operativo) es el mismo que deja la mayor cantidad de dinero (volumen financiero).
\end{enumerate}

\section{Cobertura} 

\subsection{Horizontal} 

La cobertura horizontal abarca las características intrínsecas de cada transacción registrada en la base de datos. Se consideran las dimensiones demográficas del cliente (edad y género), las dimensiones descriptivas del producto (categoría) y las métricas financieras (cantidad comprada, precio por unidad y monto total facturado).

\begin{table}[ht]
\centering
\caption{Variables consideradas en el estudio} 
\resizebox{12cm}{!} {
\begin{tabular}{cll}
  \hline
  \textbf{TIPO} & \textbf{VARIABLE} & \textbf{DESCRIPCIÓN} \\ 
  \hline
Temporal & Date & Fecha exacta en la que se efectuó la transacción \\
Categórica & Gender & Género del cliente (Male, Female) \\
Numérica & Age & Edad biológica del comprador \\
Categórica & Product Category & Categoría del artículo comprado (e.g., Beauty, Clothing) \\
Numérica & Quantity & Cantidad de artículos llevados en una misma transacción \\
Numérica & Price per Unit & Precio unitario del producto \\
Numérica & Total Amount & Monto total facturado en la compra (Ticket) \\
   \hline
\end{tabular}
}
\end{table}

\subsection{Vertical} 

La cobertura vertical del trabajo de investigación comprende un total de 1,000 observaciones (registros) válidos, tras el proceso de limpieza y omisión de variables no analíticas (como Transaction ID y Customer ID). Cada fila representa una compra única realizada por un cliente en la tienda.

\section{Periodo de Referencia} 

Los datos analizados corresponden a un historial transaccional de un año calendario, abarcando las ventas registradas entre Enero de 2023 y Diciembre de 2023. Esto permite capturar un ciclo comercial completo y evitar sesgos de meses específicos.


\section{Variables} 

La variable principal en estudio es \textbf{Total Amount}, que representa el éxito financiero del negocio medido a través del ingreso total generado por cada transacción.

Como variables explicativas o independientes se analizan:

\begin{enumerate}
    \item Variables Categóricas: \textbf{Gender} y \textbf{Product Category}
    \item Variables Numéricas: \textbf{Age}, \textbf{Quantity} y \textbf{Price per Unit}
    \item Variables Temporales: \textbf{Date}
\end{enumerate}

\chapter{Marco Teórico} 

\section{Bases Teóricas}

El presente capítulo establece los fundamentos matemáticos y estadísticos aplicados para el análisis de los datos transaccionales del sector retail. Se aborda la teoría desde una perspectiva de inteligencia de negocios, priorizando los conceptos que permiten perfilar al cliente, medir el volumen financiero y calcular preferencias de consumo.

La comprensión de estos conceptos es el sustento metodológico de las operaciones computacionales realizadas sobre el conjunto de datos para dar respuesta a los objetivos de la investigación.

\subsection{Análisis Exploratorio de Datos (EDA)}

El Análisis Exploratorio de Datos es el paso preliminar para resumir las características principales de un conjunto de transacciones. Matemáticamente, para una variable numérica $X$ (como el monto total de la compra o \textit{Total Amount}), el análisis se fundamenta en sus métricas agregadas. Sea $x_1, x_2, \dots, x_n$ una muestra de ventas observadas, definimos:

\begin{itemize}
    \item \textbf{Esperanza Matemática (Media Muestral):} Representa el ticket promedio esperado por cada transacción realizada.
    $$ \bar{x} = \frac{1}{n} \sum_{i=1}^{n} x_i $$

    \item \textbf{Agregación de Valor (Suma Total):} Representa el volumen de ingresos acumulados, métrica indispensable para evaluar el impacto financiero real de un segmento de mercado.
    $$ Total = \sum_{i=1}^{n} x_i $$
\end{itemize}

\subsection{Estratificación y Esperanza Condicional}

Dado que el mercado minorista no es homogéneo, el análisis global de los datos oculta patrones de comportamiento. Para resolver esto, se aplica el principio de estratificación (operación de agrupamiento), dividiendo la población total en subgrupos mutuamente excluyentes basados en características categóricas como el Género o el Rango de Edad.

Matemáticamente, el cálculo del ticket promedio por segmento se evalúa mediante la \textbf{Esperanza Condicional}. Ésta calcula el valor esperado de una variable numérica (monto gastado $X$) dado que el cliente pertenece a una categoría específica $Y$.

$$ E[X \mid Y = y] = \sum x \cdot P(X=x \mid Y=y) $$

Este concepto es el núcleo de la sumarización segmentada, permitiendo comparar de forma estandarizada el valor financiero que aportan, por ejemplo, los hombres frente a las mujeres, o los clientes jóvenes frente a los adultos.

\subsection{Frecuencia Relativa y Preferencia de Consumo}

Gran parte del análisis de comportamiento del consumidor no se basa en montos, sino en la proporción en la que un segmento elige una u otra categoría (ej. Belleza vs. Electrónica). La preferencia por una categoría $A$ dentro de un segmento $B$ se aproxima mediante la frecuencia relativa condicional observada en la muestra:

$$ P(A \mid B) \approx \frac{f_{A \cap B}}{f_B} $$

Donde $f_{A \cap B}$ es el número de transacciones de la categoría $A$ hechas por el segmento $B$, y $f_B$ es el total de compras de dicho segmento. La normalización de estas frecuencias a porcentajes permite comparar hábitos de compra entre grupos de distintos tamaños de manera equitativa.

\subsection{Discretización de Variables Continuas}

Para facilitar el análisis de perfiles, las variables continuas o numéricas de amplio espectro suelen transformarse en variables categóricas ordinales. Este proceso matemático se conoce como discretización o \textit{binning}. 

En este estudio, la variable Precio por Unidad ($P$) se discretiza utilizando una función definida a trozos para clasificar el tipo de producto en "Premium" o "Económico", estableciendo un umbral de corte ($k = 300\$ $):

$$
\text{Categoría}(P) = \begin{cases} 
\text{Premium} & \text{si } P > k \\ 
\text{Económico} & \text{si } P \le k 
\end{cases}
$$

Del mismo modo, la variable Edad se transforma en Rangos Etarios mediante condiciones lógicas secuenciales. Esta transformación es vital para poder aplicar las técnicas de estratificación y frecuencia relativa descritas anteriormente, permitiendo descubrir tendencias generacionales claras.


\chapter{Marco Metódico}

En esta sección se presentan las metodologías, procedimientos algorítmicos y técnicas descriptivas implementadas para dar respuesta a los objetivos planteados en el capítulo anterior. Se detalla el tratamiento de la fuente de datos transaccional, las técnicas de limpieza (Data Wrangling), la transformación de variables y los métodos de agrupación utilizados para caracterizar el comportamiento de los clientes.

El análisis se ha realizado utilizando el lenguaje de programación \textbf{R} (versión 4.x) bajo el entorno de desarrollo RStudio, empleando el paradigma \textbf{Tidyverse} (específicamente la librería \texttt{dplyr}) para la manipulación de datos estructurados, y paquetes de visualización para la generación de los gráficos.

\section{Bases metódicas utilizadas en el trabajo de investigación}

El diseño de la investigación es de carácter \textbf{cuantitativo, no experimental y descriptivo-exploratorio}, dado que se analizan datos históricos observados sin manipulación directa de las variables. A continuación, se describen las etapas del procesamiento analítico.

\subsection{Procesamiento y Limpieza de Datos (ETL)}

La calidad de los resultados depende directamente de la integridad de los datos de entrada. El proceso de Extracción, Transformación y Carga (ETL) consistió en la inspección de valores nulos (\textit{missing values}), detección de registros duplicados y la adaptación de los tipos de datos.

Específicamente, se verificó mediante funciones de R que la base de datos original se encontraba en perfectas condiciones operativas. Para optimizar el procesamiento en memoria, se procedió a la eliminación de las columnas identificadoras (\texttt{Customer ID} y \texttt{Transaction ID}) que carecen de valor analítico, y se estandarizó la variable temporal al formato de fecha correcto (\texttt{Date}).

```{r etl-process, echo=TRUE, message=FALSE, warning=FALSE}
library(tidyverse)
library(knitr)

# 1. Carga de datos crudos
datos_retail <- read_csv("C:/Users/STRUCCO/Desktop/Rafael/Informe/Dataset/Retail.csv")

datos_retail <- datos_retail %>%
  rename(
    ID_Transaccion = `Transaction ID`,
    Fecha = Date,
    Cliente_ID = `Customer ID`,
    Genero = Gender,
    Edad = Age,
    Categoria_producto = `Product Category`,
    Cantidad = Quantity,
    Precio_por_cantidad = `Price per Unit`,
    Monto_total = `Total Amount`
  )

datos_retail <- datos_retail %>%
  mutate(Genero = case_match(Genero,
                           "Female" ~ "Mujer",
                         "Male" ~ "Hombre"),
         Categoria_producto = case_match(Categoria_producto,
                                         "Clothing" ~ "Ropa",
                                         "Electronics" ~ "Electronica",
                                         "Beauty" ~ "Belleza"))

# Convirtiendo la columna Date de texto a formato fecha

library(lubridate)
datos_retail$Fecha <- ymd(datos_retail$Fecha)

datos_limpios <- datos_retail %>%
  drop_na() %>%
  distinct()

write.csv(datos_limpios, "retail_limpio.csv", row.names = FALSE)
```

\subsection{Transformación y Discretización de Variables (Feature Engineering)}

Para habilitar el análisis segmentado (estratificación) requerido por los objetivos de la investigación, fue estrictamente necesario transformar variables numéricas continuas en variables categóricas ordinales. Este proceso de discretización se realizó empleando la función \texttt{mutate()} combinada con sentencias lógicas condicionales (\texttt{ifelse()}).

Se crearon dos nuevas dimensiones analíticas fundamentales en el conjunto de datos:
\begin{itemize}
\item \textbf{Grupo Etario:} Para contrastar el comportamiento generacional, agrupando a los clientes en "40 o Menor" y "Mayor a 40".
\item \textbf{Tipo de Producto:} Para clasificar los artículos según su \texttt{Price per Unit} en gamas "Económicas" ($\leq 300$) y "Premium" ($> 300$).
\end{itemize}

```{r feature-engineering, echo=TRUE, message=FALSE, warning=FALSE}
# Creación de variables categóricas condicionales
retail <- read_csv("retail_limpio.csv")

max(retail$Edad)
min(retail$Edad)

cortes <- c(18, 27, 36, 45, 54, 64)
etiquetas <- c("Adulto joven (18-26)",
               "Adulto (27-35)",
               "Adulto Maduro (36-43)",
               "Mediana edad (44-53)",
               "Adulto Mayor (54-64)")

retail["Rango_edad"] <- cut(retail$Edad,
                   breaks = cortes,
                   labels = etiquetas,
                   include.lowest = TRUE)
# Acumulado de gasto por mes

datos_mensual <- retail %>%
  mutate(mes = floor_date(Fecha, "month")) %>%
  group_by(mes, Rango_edad) %>%
  summarise(gasto_por_mes = sum(Monto_total), .groups = "drop")
 
# Gasto acumulado al pasar de los meses

datos_mensual <- datos_mensual %>%
  arrange(mes) %>%
  group_by(Rango_edad) %>%
  mutate(Gasto_acumulado = cumsum(gasto_por_mes)) %>%
  ungroup() %>%
  filter(mes != "2024-01-01")

# Para saber el porcentaje por la compra de categoria de edad por segmento etario

preferencias <- retail %>%
  group_by(Genero, Rango_edad, Categoria_producto) %>%
  summarise(total_elecciones = n(), .groups = "drop") %>%
  mutate(Porcentaje_preferencia = (total_elecciones / sum(total_elecciones) * 100))

retail <- retail %>%
  mutate(segmento = ifelse(Monto_total >= 300, "Premium", "Estandar"))

perfiles_premium <- retail %>%
  group_by(Rango_edad, segmento) %>%
  summarise(
    Total_ventas = n(),
    Monto_total = sum(Monto_total),
    .groups = 'drop'
  ) %>%
  group_by(Rango_edad) %>%
  mutate(Porcentaje_por_segmento = (Monto_total / sum(Monto_total)) * 100) %>%
  ungroup() %>%
  mutate(Porcentaje_global = (Monto_total / sum(Monto_total)) * 100)
  
```

\chapter{Análisis de Resultados}

\small Este capítulo presenta los resultados obtenidos al aplicar las metodologías descritas en el capítulo anterior. Se expone el análisis estadístico descriptivo de las variables estudiadas, validando las hipótesis de negocio formuladas respecto al comportamiento demográfico y financiero de los clientes.

```{r setup-results, include=FALSE}
# Aseguramos que las librerías estén cargadas
library(tidyverse)
library(kableExtra)
library(scales)

```

\section{Ticket Promedio por Género}
El primer objetivo del análisis consistió en determinar si existía una variación significativa en el ticket promedio de compra (\textit{Total Amount}) entre hombres y mujeres, lo cual es vital para la asignación de presupuestos de marketing.
  
Como se evidencia en el Cuadro \ref{tab:obj1_table}, el ticket promedio no varía significativamente entre géneros (Mujeres: \$456.55 vs Hombres: \$455.43). La diferencia es mínima, lo que indica que el género no es un factor determinante en la disposición de gasto por visita.

```{r obj1_plot_barras, echo=FALSE, fig.cap="Ticket Promedio de Compra por Género", fig.height=6, out.width="100%", fig.width = 10}

ggplot(retail,
       aes(x = Categoria_producto,
           y = Monto_total,
           fill = Genero)) +
  geom_col(color = NA) +

  # Facetas
  facet_wrap(~Genero, scales = "free_y") +

  # Escalas
  scale_y_continuous(expand = expansion(mult = c(0, 0.10)),
                     labels = scales::label_dollar(big_mark = ",")) +
  scale_fill_manual(
    values = c("Mujer" = "#4e3159", "Hombre" = "#0a1e36")
  ) +

  labs(title = "Análisis de gasto total",
       subtitle = "Compras hechas por hombres y mujeres separados por categoria",
       y = "Acumulado del monto total",
       x = "Producto comprado",
       fill = "Genero") +
  theme_minimal(base_size = 15) +
  theme(
    # Ajustando el titulo para que sea más legible
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", size = 20, hjust = 0.5, color = "#2c3e50", margin = margin(b = 10)),
    plot.subtitle = element_text(face = "bold", size = 14, hjust = 0.5, margin = margin(b = 15), color = "#5a6366"),
    plot.margin = margin(t = 10, r = 10, b = 10, l = 0),
    
    #Ajustando la leyenda para dejarle más espacio al gráfico
    legend.position = "bottom",
    legend.title = element_text(face = "bold", size = 12),
    legend.text = element_text(size = 12, face = "bold"),
    legend.box.spacing = unit(0, "pt"),
    

    panel.grid.major.x = element_blank(), # Eliminando las barras verticales para la limpieza visual
    
    # Ajustando las etiquetas para una mejor estética
    strip.background = element_rect(fill = "#babbcf", color = NA),
    strip.text = element_text(face = "bold", size = 12),
    
    # Limpiando el texto de los ejes para la limpieza visual
    axis.text.x = element_text(face = "bold", size = 10),
    axis.title.x = element_text(size = 14, face = "bold", margin = margin(t = 10)),
    axis.title.y = element_text(size = 14, face = "bold", margin = margin(r = 15), hjust = 0.5),
    axis.text = element_text(size = 11, color = "#424042"),
    axis.line.x = element_line(color = "#424042"))

```


```{r obj1_plot_boxplot, echo=FALSE, fig.cap="Ticket Promedio de Compra por Género", fig.height=6, out.width="100%", fig.width=10}

ggplot(retail,
       aes(y = Monto_total,
           fill = Genero)) +
  geom_boxplot(color = "black") +
  labs(title = "Análisis de la distribución del gasto total",
       subtitle = "Distribución del gasto total de hombres y mujeres",
       y = "Total gastado"
  ) +
  
  # Facetas
  facet_wrap(~Genero) +
  
  # Ajuste de las escalas
  scale_y_continuous(
    labels = scales::label_dollar(bigmark = ",")
  ) +
  scale_fill_manual(
    values = c(Mujer = "#4e3159", Hombre = "#0a1e36")
  ) +
  
  theme_minimal() + 
  theme(
    
    # Ajustes al titulo y al subtitulo para que sea más legible
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", hjust = 0.5, color = "#2c3e50", size = 20),
    plot.subtitle = element_text(face = "bold", hjust = 0.5, color = "gray20", size = 14),
    
    # Ajustando a la leyenda para dejar más espacio al gráfico
    legend.position = "bottom",
    legend.title = element_text(size = 12, face = "bold"),
    legend.text = element_text(size = 12, face = "bold"),
    legend.box.spacing = unit(0, "pt"),
    
    # Ajustes a las etiquetas "Hombre" y "Mujer" para que sean más legibles
    strip.background = element_rect(fill = "#babbcf", color = NA),
    strip.text = element_text(face = "bold", size = 12),
    
    panel.grid.minor = element_blank(),
    panel.grid.major.y = element_blank(),
    
    # Ajustes al texto de los ejes para la limpieza visual
    axis.title.y = element_text(size = 14, face = "bold"),
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank())

```


```{r obj1_plot_densidad, echo=FALSE, fig.cap="Ticket Promedio de Compra por Género", fig.height=3.5, out.width="100%", fig.width = 10}
  
ggplot(retail,
       aes(x = Monto_total,
           fill = Genero
           )) +
  
  geom_density(adjust = 1.5, color = NA) +
  
  # Facetas
  facet_wrap(~Genero) +
  
  # Ajuste en las escalas
  scale_x_continuous(
    labels = scales::label_dollar(big.mark = ",")
  ) +
  scale_fill_manual(
    values = c(Mujer = "#4e3159", Hombre = "#0a1e36")
  ) +
  labs(title = "Relación entre la frecuencia de compra y el gasto total por género",
       subtitle = "Comparativa del comportamiento de gasto de la segmentación género",
       y = "Densidad",
       x = "Gasto total") +
  theme_minimal() + 
  theme(
    
    # Ajuste al título para que esté más centrado y más legible
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", size = 15, hjust = 0.5, color = "#2c3e50"),
    plot.subtitle = element_text(face = "bold", size = 13, hjust = 0.5, color = "gray20"),
    
    # Ajustando la leyenda para abajo para darle más espacio al gráfico
    legend.position = "bottom",
    legend.title = element_text(size = 12, face = "bold"),
    legend.text = element_text(size = 12, face = "bold"),
    legend.box.spacing = unit(0, "pt"),
    
    # Ajustando las etiquetas para la limpieza visual
    strip.background = element_rect(fill = "#babbcf", color = NA),
    strip.text = element_text(size = 10, face = "bold"),
    
    # Ajustando los titulos y textos en "x" y "y" para la limpieza visual
    axis.text.y = element_text(size = 10, face = "bold"),
    axis.text.x = element_text(size = 10, face = "bold"),
    axis.title.y = element_text(size = 15, face = "bold", margin = margin(r = 10)),
    axis.title.x = element_text(size = 15, face = "bold", margin = margin(t = 10)),
    
    # Eliminando las lineas horizontales y verticales para una mejor claridad visual
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank())

```

\section{Análisis del gasto acumulado por Segmento Etario}
Al segmentar la base de datos entre clientes jóvenes (40 años o menos) y adultos (mayores de 40), se buscó entender cómo la edad condiciona la preferencia por ciertas categorías.

```{r obj2_plot_barras, echo=FALSE, fig.cap="Preferencias de Categoría según Grupo Etario", fig.height=6, out.width="100%", fig.width = 10}

ggplot(retail,
       aes(x = Rango_edad,
           y = Monto_total,
           fill = Rango_edad)) +
  geom_col() +
  scale_y_continuous(
    labels = scales::dollar_format(bigmark = ","),
    expand = expansion(mult = c(0, 0.20))
  ) +
  scale_x_discrete(expand = expansion(mult = c(0.1, 0.1))) + # Para separar más los textos de abajo
  scale_fill_manual(
    values = c("Adulto joven (18-26)" = "#152b45", 
               "Adulto (27-35)" = "#183557",
               "Adulto Maduro (36-43)" = "#19406e",
               "Mediana edad (44-53)" = "#1759a6",
               "Adulto Mayor (54-64)" = "#096fe6") 
  ) +
  labs(title = "Gasto acumulado por grupo de edad",
       subtitle = "Gasto acumulado en el año 2023 por segmento Etario",
       x = "Segmento Etario",
       y = "Total gastado") +
  theme_minimal() + 
  theme(
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", size = 15, hjust = 0.5, color = "#081a26"),
    plot.subtitle = element_text(face = "bold", size = 12, hjust = 0.5, color = "#0c293d"),
    
    legend.position = "none",
    
    axis.title.y = element_text(face = "bold", size = 15),
    axis.title.x = element_text(face = "bold", size = 15, margin = margin(t = 10)),
    axis.text = element_text(color = "black"),
    axis.text.x = element_text(color = "black", size = 10),
    
    panel.grid.major.x = element_blank(),
    panel.grid.minor = element_blank()
  ) 

```


```{r obj2_plot_lineas, echo=FALSE, fig.cap="Preferencias de Categoría según Grupo Etario", fig.height=4, out.width="100%", fig.width = 10}

ggplot(datos_mensual,
       aes(x = mes,
           y = Gasto_acumulado,
           color = Rango_edad)) +
  geom_line() +
  geom_point() +
  scale_y_continuous(labels = scales::label_dollar(bigmark = ",")) +
  scale_x_date(
    date_labels = "%b",
    date_breaks = "1 month") + # Para que el grafico solo evalue el gasto acumulado al final de mes
  scale_color_manual(
    values = c("Adulto joven (18-26)" = "#283D3B", 
               "Adulto (27-35)" = "#197278",
               "Adulto Maduro (36-43)" = "#C4A08C",
               "Mediana edad (44-53)" = "#C44536",
               "Adulto Mayor (54-64)" = "#501C15")
  ) +
  labs(title = "Tendencia de compras mensuales acumuladas por segmento Etario",
       subtitle = "Análisis de compra mensuales acumuladas por segmento Etario en el año 2023",
       x = "Mes",
       y = "Gasto acumulado",
       color = "Rangos de edad") +
  theme_minimal() +
  theme(
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", size = 15, hjust = 0.5, color = "#081a26"),
    plot.subtitle = element_text(face = "bold", size = 12, hjust = 0.5, color = "#152d4f"),
    
    legend.position = "bottom",
    legend.title = element_text(face = "bold", size = 9),
    legend.text = element_text(face = "bold", size = 9),
    legend.box.spacing = unit(0, "pt"),
    
    axis.title.y = element_text(face = "bold", size = 12),
    axis.title.x = element_text(face = "bold", size = 12),
    axis.text = element_text(color = "black")
  ) +
  guides(color = guide_legend(nrow = 2, byrow = TRUE, title.position = "left"))

```


```{r obj2_plot_boxplot, echo=FALSE, fig.cap="Preferencias de Categoría según Grupo Etario", fig.height=4,  out.width="100%", fig.width = 10}

ggplot(retail,
       aes(x = Rango_edad,
           y = Monto_total,
           fill = Rango_edad)) +
  geom_boxplot(color = "black") + 
  scale_y_continuous(labels = scales::label_dollar(bigmark = ",")) +
  scale_fill_manual(
    values = c("Adulto joven (18-26)" = "#003F5D", 
               "Adulto (27-35)" = "#00527C",
               "Adulto Maduro (36-43)" = "#00609C",
               "Mediana edad (44-53)" = "#006DB2",
               "Adulto Mayor (54-64)" = "#4E97D1")
  ) + 
  labs(title = "Distribución del monto por ticket por segmento Etario",
       subtitle = "Análisis de la distribución del gasto acumulado por grupo Etario en el año 2023",
       x = "Segmento Etario",
       y = "Gasto acumulado") +
  theme_minimal() +
  theme(
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", size = 15, hjust = 0.5, color = "#182942"),
    plot.subtitle = element_text(face = "bold", size = 12, hjust = 0.5, color = "#152d4f"),
    
    legend.position = "bottom",
    legend.title = element_text(face = "bold", size = 9),
    legend.text = element_text(face = "bold", size = 9),
    legend.box.spacing = unit(0, "pt"),
    
    axis.title.y = element_text(face = "bold", size = 12),
    axis.title.x = element_text(face = "bold", size = 12),
    axis.text = element_text(color = "black"),
    
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank()
  ) 

```
La Figura \ref{fig:obj2_plot} revela un hallazgo clave: mientras que los clientes de 40 años o menos distribuyen sus compras de forma casi equitativa (aprox. 33% en cada categoría), los clientes mayores de 40 años pierden interés en la categoría de \textit{Beauty} (cayendo al 28.8%), enfocando su consumo en \textit{Clothing} y \textit{Electronics}.
\section{Tendencia de Compra: Económico vs Premium}
Se contrastó la hipótesis general de que, a mayor edad, los clientes tienen una mayor tendencia a adquirir artículos "Premium" (precio unitario > \$300).

```{r obj3_plot, echo=FALSE, fig.cap="Proporción de compras Premium vs Económico por Edad", fig.height=6, out.width="100%", fig.width = 10}

```
La visualización de barras apiladas al 100% (Figura \ref{fig:obj3_plot}) refuta la hipótesis inicial. El segmento más joven (18-25 años) presenta la mayor proporción de compras Premium. Por el contrario, el grupo de adultos de 36 a 50 años es el más conservador, destinando casi el 82% de sus transacciones a artículos económicos.
\section{Volumen de Ingresos Acumulados por Rango de Edad}
A pesar de que los jóvenes compran mayor porcentaje de artículos Premium, el análisis del ingreso total revela quién sostiene financieramente el negocio.

```{r obj4_plot_calor, echo=FALSE, fig.cap="Ingresos Acumulados por Rango de Edad", fig.height=6, out.width="100%", fig.width = 10}

ggplot(preferencias,
       aes(x = Categoria_producto,
           y = Rango_edad,
           fill = Porcentaje_preferencia)) +
  geom_tile(color = "black", linewidth = 0.1) +
  geom_text(aes(label = sprintf("%.2f%%", Porcentaje_preferencia)),
            color = ifelse(preferencias$Porcentaje_preferencia > 3.25, "black", "white"),
            size = 3, fontface = "bold",) +
  facet_wrap(~Genero) +
  scale_fill_gradient2(
    low = "#02093d",
    mid = "gray90",
    high = "#941929",
    midpoint = 3.25,
    labels = function(x) paste0(x, "%")
  ) + 
  labs(title = "Intensidad de preferencia",
       subtitle = "Intensidad de preferencia por segmento demográfico en el año 2023",
       y = "Segmento Etario",
       x = "Categoria de Producto",
       fill = "Porcentaje de preferencia") +
  theme_minimal() +
  theme(
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", size = 15, hjust = 0.5, color = "#07073d"),
    plot.subtitle = element_text(face = "bold", size = 12, hjust = 0.5, color = "#08084d"),
 
    legend.title = element_text(face = "bold", size = 9, hjust = 0.5),
    legend.text = element_text(face = "bold", size = 9, hjust = 0.5),
    legend.title.align = 0.5,
    
    strip.background = element_rect(fill = "gray80", color = NA),
    strip.text = element_text(face = "bold", size = 12),
    
    axis.title.x = element_text(face = "bold", size = 13),
    axis.title.y = element_text(face = "bold", size = 13),
    axis.text = element_text(face = "plain", color = "black"),
    
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_blank()
  ) 

```

El grupo de 36 a 50 años se consolida como la "Vaca Lechera" (\textit{Cash Cow}) de la empresa, aportando el máximo de ingresos con \$139,660. Aunque compran artículos más económicos, su volumen de transacciones compensa la diferencia, superando financieramente a los segmentos más jóvenes.
\section{Correlación entre Volumen Operativo y Financiero}
Finalmente, se cruzaron las variables de Rango de Edad y Género para identificar de manera exacta el \textit{Buyer Persona} principal de la tienda, evaluando si el segmento que realiza más transacciones es el que genera mayores ingresos.

```{r obj5_table, echo=FALSE, fig.height=6, out.width="100%", fig.width = 10}

ggplot(perfiles_premium,
       aes(x = Rango_edad,
           y = Monto_total,
           fill = segmento)) +
  geom_bar(stat = "identity", position = "dodge") +
  scale_y_continuous(labels = scales::label_dollar(bigmark = ","),
                     expand = expansion(mult = c(0, 0.05))) +
  scale_x_discrete(expand = expansion(mult = c(0.1, 0.1))) +
  scale_fill_manual(
    values = c(Estandar = "#436EEE", Premium = "#27408B")) +
  labs(title = "Contraste de Total gastado por Rango de edad y segmentación",
       subtitle = "Análisis del contraste que hay entre las compras Premium y el Rango de edad en el año 2023",
       x = "Segmento Etario",
       y = "Total gastado",
       fill = "Segmento:") +
  theme_minimal() +
  theme(
    plot.title.position = "plot",
    plot.title = element_text(face = "bold", size = 15, hjust = 0.5),
    plot.subtitle = element_text(face = "bold", size = 12, hjust = 0.5),
    
    legend.position = "bottom",
    legend.title = element_text(face = "bold", size = 9),
    legend.text = element_text(face = "bold", size = 9),
    legend.box.spacing = unit(0, "pt"),
    
    axis.title.x = element_text(face = "bold", size = 14, margin = margin(t = 10)),
    axis.title.y = element_text(face = "bold", size = 14),
    axis.text = element_text(face = "plain", color = "black"),
    axis.line.x = element_line(color = "#4876FF", size = 0.8),
    
    panel.grid.major.x = element_blank(),
    panel.grid.minor = element_blank()
  )

```
Interpretación de Resultados:
\begin{itemize}
\item Existe una correlación directa y positiva entre el volumen operativo y el financiero. El segmento de \textbf{Mujeres de 36 a 50 años} lidera ambas métricas de forma absoluta (164 transacciones y \$70,790 en facturación).
\item Se observa un cambio de comportamiento generacional: en la juventud (18-25), los hombres son los principales compradores, pero a partir de los 26 años, el público femenino domina indiscutiblemente las métricas de consumo.
\end{itemize}
Estos resultados validan la necesidad de optimizar las estrategias de \textit{Customer Journey} y \textit{Visual Merchandising} para satisfacer las preferencias del público femenino adulto, mientras se diseñan campañas de retención para hombres jóvenes.

\chapter{Conclusiones y Recomendaciones}

El presente estudio ha permitido caracterizar el comportamiento de compra y la rentabilidad de los clientes en el sector retail mediante el análisis descriptivo de un historial transaccional anual. A continuación, se presentan las conclusiones derivadas de la evidencia estadística y las recomendaciones estratégicas para la optimización de los recursos comerciales.

\textbf{1.-} Se evidenció que el género del cliente no es un factor diferenciador en la disposición de gasto (Ticket Promedio aproximado de \$456 para ambos). Sin embargo, al cruzar la edad con la categoría de producto, los clientes mayores de 40 años muestran un desinterés marcado por la categoría de Belleza (\textit{Beauty}), concentrando su consumo casi exclusivamente en Ropa (\textit{Clothing}) y Electrónica (\textit{Electronics}).

\textbf{Recomendación:} 
Para la gestión de presupuestos de marketing, se recomienda una segmentación rigurosa de las campañas. Los esfuerzos de publicidad para productos de Belleza deben redirigirse exclusivamente al segmento joven (40 años o menor), evitando el desperdicio de capital de inversión publicitaria (\textit{Ad-Spend}) en el público adulto, donde la tasa de conversión esperada es estadísticamente menor.

\textbf{2.-} Se refutó la hipótesis de que a mayor edad existe una mayor preferencia por artículos "Premium" ($> \$300$). El segmento más joven (18-25 años) lidera la proporción de compras de alto valor. No obstante, el grupo de adultos de 36 a 50 años se consolida como el motor financiero (\textit{Cash Cow}) de la empresa, generando el mayor volumen de ingresos acumulados (\$139,660) mediante un alto volumen operativo de productos económicos.

\textbf{Recomendación:} 
Para la estrategia de fijación de precios (\textit{Pricing}) y ventas cruzadas (\textit{Cross-Selling}), se recomienda enfocar la oferta de productos Premium y tácticas de \textit{Up-Selling} en el segmento de jóvenes adultos. Paralelamente, es imperativo implementar programas de fidelización agresivos para el grupo de 36 a 50 años, garantizando la retención de la base de clientes que sostiene la liquidez y el flujo de caja operativo del negocio.

\textbf{3.-} Existe una convergencia perfecta entre el volumen operativo (tráfico de transacciones) y el volumen financiero (ingresos). El micro-segmento de \textbf{Mujeres entre 36 y 50 años} lidera ambas métricas de forma absoluta. Adicionalmente, se detectó una transición demográfica importante: los hombres dominan el volumen de consumo en la juventud (18-25), pero las mujeres asumen el liderazgo a partir de los 26 años.

\textbf{Recomendación:} 
El diseño de la experiencia de compra en tienda (\textit{Customer Journey}) y el \textit{Visual Merchandising} deben pivotar en torno a las preferencias de las Mujeres de 36 a 50 años, asegurando un surtido de inventario óptimo para ellas. Asimismo, se deben diseñar campañas de reactivación dirigidas a los hombres jóvenes para mitigar su tasa de abandono (\textit{Churn Rate}) al entrar a la etapa de adultez.

\appendix
\renewcommand{\thechapter}{\Alph{chapter}}

\chapter{Ranking de Transacciones de Mayor Valor}

En este anexo se presenta el listado desglosado de las 20 transacciones individuales con mayor monto facturado (\textit{Total Amount}) del histórico, permitiendo auditar los valores atípicos y compras de alto volumen.

```{r tabla-top20, echo=FALSE, message=FALSE, warning=FALSE}

```

\chapter{Visualización de Densidad de Mercado}
Para complementar el análisis demográfico, se presenta un mapa de calor (\textit{Heatmap}) que cruza las dos variables principales de segmentación: Categoría del Producto y Rango de Edad. La intensidad del color representa la suma total de ingresos generados.

```{r heatmap-anexo, echo=FALSE, fig.height=4, fig.cap="Concentración de Ingresos por Edad y Categoría"}

```

\chapter{Diagnóstico de Residuos del Modelo Lineal}
Como soporte estadístico adicional, se presentan los gráficos de diagnóstico para validar los supuestos de un modelo de regresión lineal simple (OLS) donde se intenta explicar el Monto Total Facturado (\textit{Total Amount}) en función de la Cantidad de artículos comprados (\textit{Quantity}).

$$ \text{Total\_Amount} = \beta_0 + \beta_1 (\text{Quantity}) + \epsilon $$
```{r diag-residuos, echo=FALSE, fig.height=4}

```
Como se observa en el gráfico Q-Q (derecha), los residuos se desvían significativamente de la línea diagonal punteada, formando una curva con colas pesadas. Esto confirma que los errores no siguen una distribución normal, validando estadísticamente que el monto total de una compra no aumenta de manera estrictamente lineal con la cantidad de artículos físicos, debido a la altísima variabilidad que introduce la mezcla de productos "Económicos" y "Premium" en un mismo carrito.
```
